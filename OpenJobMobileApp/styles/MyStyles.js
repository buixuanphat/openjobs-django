import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        flex: 1,
        // justifyContent: 'center',
        // alignItems: 'center'
        marginTop: 60,
    },
    row:{
        flexDirection: "row",
        flexWrap:"wrap",
    },
    margin:{
        margin:5,
    }
})